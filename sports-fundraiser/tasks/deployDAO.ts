import { task } from 'hardhat/config';

task('deployDAO', 'Deploy Everything').setAction(async ({ }, hre) => {
    //hre.tasks["auction"].action.apply
    // await hre.run("deploy-modules")
    // await hre.run("deploy-profile-holder")
    // await hre.run("post-memes")
    // await hre.run("like")
    // await hre.run("mock-chainlink-keeper")
    // await hre.run("post-memes")
    // await hre.run("like")
    // await hre.run("auction")
    // await hre.run("mock-chainlink-keeper")
})